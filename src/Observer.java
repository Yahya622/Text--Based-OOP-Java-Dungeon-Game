public interface Observer { //observer interface
    //observer pattern used
    void update(String event);  //interface method signature defines the rules any class which implemets observer must follow as observer is the interface
}